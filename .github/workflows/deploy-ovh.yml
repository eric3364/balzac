name: Deploy to OVH (FTPS)

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: npm install

      - name: Build
        run: npm run build

      - name: Prepare deploy folder
        run: |
          mkdir -p deploy
          cp -r dist/* deploy/
          # placer .htaccess à la racine du site
          if [ -f public/.htaccess ]; then cp public/.htaccess deploy/.htaccess; fi

            - name: Deploy via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}     # ftp.cluster100.hosting.ovh.net
          username: ${{ secrets.FTP_USER }}     # balzaci
          password: ${{ secrets.FTP_PASSWORD }} # mot de passe FTP
          protocol: ftps                        # <-- passe en FTPS "normal"
          port: ${{ secrets.FTP_PORT }}         # 21
          local-dir: ./deploy/
          server-dir: www/
          dangerous-clean-slate: true
          exclude: |
            **/.git*
            **/node_modules/**
            **/.github/**

            **/.git*
            **/node_modules/**
            **/.github/**
